import styled from "styled-components";
import {ParagraphProps} from "@/utils/declarations/Styles";

export const IconWrapper = styled.div<ParagraphProps>`
  margin: 0;
  padding: 0;
  display: flex;
  cursor:pointer;
`